export interface RoundedHeaderSectionProps {
  children: React.ReactNode;
  testID?: string;
}

export interface RoundedHeaderSectionTheme {
  backgroundColor: string;
  borderRadius: number;
  paddingVertical: number;
}
