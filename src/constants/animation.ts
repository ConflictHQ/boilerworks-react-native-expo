import { FadeIn, FadeOut, LinearTransition } from 'react-native-reanimated';

export const DEFAULT_FADE_IN = FadeIn.duration(240);
export const DEFAULT_FADE_OUT = FadeOut.duration(240);
export const DEFAULT_LAYOUT_TRANSITION = LinearTransition.duration(360);
